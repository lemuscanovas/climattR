% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bs_analogs.R
\name{bs_analogs}
\alias{bs_analogs}
\title{Bootstrap Analogs for Time Series Data}
\usage{
bs_analogs(
  x,
  analogs,
  n = 1000,
  event_fun = "mean",
  anom = F,
  ref_period = NULL,
  replace = T,
  detrend = F,
  k = 2
)
}
\arguments{
\item{x}{A dataframe with at least 'time' and one variable column.}

\item{analogs}{A dataframe containing analogs information including 'time_obj' and 'period'.}

\item{n}{Integer; the number of bootstrap samples to generate for each period.}

\item{event_fun}{String; the name of the function to apply during event aggregation.}

\item{anom}{Logical; indicates whether anomalies relative to a reference period should be computed.}

\item{ref_period}{Numeric vector; the start and end years of the reference period for anomaly calculations.}

\item{replace}{Logical; indicates whether sampling should be with replacement.}

\item{detrend}{Logical; indicates whether the data should be detrended.}

\item{k}{Integer; the degree of polynomial used for detrending.}
}
\value{
A list containing three elements: observed data (aggregated original data), 
        bootstrap simulation results, and a summary of bootstrap results.
}
\description{
This function performs bootstrap simulations on time series data based on specified analog periods.
It includes options for detrending, calculating anomalies, and customizing bootstrap sampling.
}
\examples{
data <- data.frame(time = seq(as.Date('2020-01-01'), by = 'day', length.out = 365),
                   var = rnorm(365))
analogs <- data.frame(time_obj = seq(as.Date('2020-01-01'), by = 'month', length.out = 12),
                      period = rep('2020', 12))
results <- bs_analogs(data, analogs, n = 100, event_fun = "mean", anom = TRUE, ref_period = c(2019, 2021))
}
